<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="bill_receipt_from_tax_invoice_view_form" model="ir.ui.view">
        <field name="name">bill.receipt.from.tax.invoice.view.form</field>
        <field name="model">bill.receipt.from.tax.invoice</field>
        <field name="arch" type="xml">
            <form string="Create Vendor Bill / Purchase Receipt">
                <p class="oe_grey">
                    Vendor Bill/Purchase Receipt will be created in draft so that you can review
                    them before validation.
                </p>
                <group>
                    <field name="move_type" widget="radio" />
                </group>
                <footer>
                    <button name="create_bill_receipt" context="{'open_bill_receipt': True}" string="Create and View" type="object" class="btn-primary"/>
                    <button name="create_bill_receipt" string="Create" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_view_bill_receipt_from_tax_invoice" model="ir.actions.act_window">
        <field name="name">Create Bill/Receipt</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">bill.receipt.from.tax.invoice</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <!-- TODO: check if we need this -->
        <field name="binding_model_id" ref="ntp_cne.model_tax_invoice" />
        <field name="binding_view_types">list</field>
    </record>

</odoo>