<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sale_order_form_inherit_ntp_communications" model="ir.ui.view">
        <field name="name">sale.order.view.form.inherit.ntp.communications</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button string="Request Invoicing"
                        name="button_notify_to_create_invoice"
                        type="object" attrs="{'invisible': [('invoice_status', '!=', 'to invoice')]}"
                        class="oe_highlight" />
            </xpath>
        </field>
    </record>

</odoo>
